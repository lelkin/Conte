/*
Base App class
*/

// global object
// Here use DrawApp app, but in base program use App app

class App{  
  App(){}
  
  void down(int i, PVector[] p, long l, int ii){}
  
  void move(PVector[] p, long l, int i){}
  
  void up(PVector[] p, long l, int i){}
  
  void cancel(PVector[] pts, long id){}
  
  void cancel(){}
}
